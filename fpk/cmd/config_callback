#!/bin/bash
### 修改配置后执行：读回上次配置 → 重新渲染 compose → 重建容器（不重建镜像）。
source "$(dirname "$0")/common"
: > "${WARN_FILE}" 2>/dev/null

load_conf
render_compose
ensure_dir "${DATA_DIR}"
chmod -R 777 "${DATA_DIR}" 2>/dev/null

recreate_container

exit 0
